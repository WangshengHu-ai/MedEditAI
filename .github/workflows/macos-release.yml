name: Build & Package macOS App

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: macos-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-archive:
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Show Environment Info
        run: |
          sw_vers
          xcodebuild -version
          swift --version
          security find-identity -v -p codesigning || true

      - name: Set up Xcode Target Configuration
        run: |
          # Ensure resolving and fetching SwiftPM dependencies
          xcodebuild -resolvePackageDependencies -project MedEditAI.xcodeproj -scheme MedEditAI

      - name: Build and Archive
        run: |
          # Build a release archive for macOS using ad-hoc / local code signing
          xcodebuild archive \
            -project MedEditAI.xcodeproj \
            -scheme MedEditAI \
            -configuration Release \
            -archivePath build/MedEditAI.xcarchive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Package App and Samples as ZIP
        run: |
          # Create packaging directory
          mkdir -p dist
          
          # Copy the standalone compiled .app bundle
          cp -R build/MedEditAI.xcarchive/Products/Applications/MedEditAI.app dist/
          
          # Also package the real PFA Samples (Excel worklists, category trees, slide templates)
          # so users have them ready-to-test directly upon downloading the installer bundle.
          cp -R Samples dist/
          
          # Compress everything into the final distribution ZIP package
          cd dist
          zip -r -y -q ../MedEditAI-macOS.zip MedEditAI.app Samples
          cd ..

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: MedEditAI-macOS-Installer
          path: MedEditAI-macOS.zip
          if-no-files-found: error
          retention-days: 7
